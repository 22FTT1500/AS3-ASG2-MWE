import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AS3-ASG2-mwe',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
